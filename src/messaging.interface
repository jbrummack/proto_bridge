syntax = "proto3";
package ffi.messaging;
// Sample messages.

class HelloWorld {
    greet (name: FullName) -> (Greeting);
    greet_in_2sec (name: FullName) async -> (Greeting);
}

class Math {
    add (v1:i32, v2: i32) -> (i64);
}

message add {
    int32 v1 = 1;
    int32 v2 = 2;
}
